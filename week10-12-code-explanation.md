# Week10〜Week12 課題コード詳解

このノートは、`week10`、`week11`、`week12` の Python 課題コードを、ファイルごとに「何をするコードか」「処理の流れ」「重要な文法・考え方」「注意点」に分けて説明したものです。

## 全体で共通する読み方

- 先頭の `""" ... """` は docstring です。そのファイルや関数の目的をメモする文字列です。
- `def 関数名(...):` は関数定義です。すぐには実行されず、あとで呼び出されたときに中身が動きます。
- `if __name__ == "__main__":` は「このファイルを直接実行したときだけ動かす」ための書き方です。他のファイルから import されたときにテスト用の `print()` が勝手に動かないようにします。
- `for _ in range(n):` の `_` は「ループ変数の値は使わない」という意味でよく使われます。
- `__file__` は実行中の `.py` ファイル自身のパスです。`__file__.replace("今のファイル名", "読みたいファイル名")` で、同じフォルダ内の別ファイルを指定しています。

---

# Week10

Week10 は主にリスト、文字列、二分探索、ランダムシミュレーションの練習です。

## `week10/10-1-1.py`：合計を求める `total(numbers)`

### 目的

`numbers` に入っている数をすべて足して、その合計を返します。Python の組み込み関数 `sum()` を自分で作る練習です。

### 処理の流れ

1. `result = 0` で、合計を入れる変数を 0 から始めます。
2. `for n in numbers:` で、リストや `range` の中身を左から順に取り出します。
3. `result = result + n` で、今までの合計に現在の数 `n` を足します。
4. すべて足し終わったら `return result` で合計を返します。

### 例

`total([1, 2, 3, 4, 5])` の場合、`result` は `0 -> 1 -> 3 -> 6 -> 10 -> 15` と変化し、最後に `15` を返します。

### ポイント

- `numbers` はリストだけでなく、`range(10)` のような反復可能なものでも使えます。
- 空リスト `[]` を渡すと、ループが一度も動かないので `0` が返ります。

## `week10/10-1-2.py`：累積和を作る `cumulative(numbers)`

### 目的

各位置までの合計をリストにして返します。たとえば `[1, 2, 3, 4, 5]` から `[1, 3, 6, 10, 15]` を作ります。

### 処理の流れ

1. `result = []` で、答えを入れる空リストを作ります。
2. `running = 0` で、「ここまでの合計」を 0 から始めます。
3. 各 `n` について `running = running + n` で合計を更新します。
4. `result.append(running)` で、その時点の合計を答えリストに追加します。
5. 最後に `result` を返します。

### 例

`[1, 2, 3]` の場合：

- 1 を読む：`running = 1`、`result = [1]`
- 2 を読む：`running = 3`、`result = [1, 3]`
- 3 を読む：`running = 6`、`result = [1, 3, 6]`

### ポイント

- `append()` はリストの末尾に 1 個の要素を追加します。
- `result` と `running` の役割が違います。`running` は現在の合計、`result` は履歴です。

## `week10/10-1-3.py`：入れ子リストの整数を合計する `nested_sum(n)`

### 目的

リストの中にさらにリストがあるような入れ子構造から、整数だけを取り出して合計します。

### 処理の流れ

1. `if type(n) is int:` で、`n` が整数そのものか調べます。
2. 整数なら、それ以上分解せず `return n` します。
3. `elif type(n) is list:` で、`n` がリストか調べます。
4. リストなら `result = 0` から始め、各 `item` に対して `nested_sum(item)` を呼び直します。
5. `None`、文字列、真偽値など、整数でもリストでもないものは `return 0` で無視します。

### 再帰の考え方

この関数は自分自身を呼び出します。これを再帰と呼びます。リストの深さを自分で数えるのではなく、「中身 1 個分の処理」を同じ関数に任せています。

### 例

`[None, 1, [2, "two", [[[[3, False, 4]]]], []], 5]` では、整数として足されるのは `1, 2, 3, 4, 5` だけなので合計は `15` です。

### 注意点

- `type(False) is int` は `False` です。Python では `bool` は `int` に似た性質がありますが、このコードでは `type(...) is int` と厳密に見ているので `False` は足されません。
- `isinstance(False, int)` を使うと `True` になるので、この課題の意図とは違う結果になります。

## `week10/10-1-4.py`：昇順か調べる `is_ordered(things)`

### 目的

リストの要素が左から右へ小さい順、または同じ値を含む非減少順になっているかを調べます。

### 処理の流れ

1. `range(len(things) - 1)` で、隣同士を比べるための添字 `i` を作ります。
2. `things[i] > things[i + 1]` なら、左の方が右より大きいので順番が崩れています。
3. 崩れを見つけた瞬間に `return False` します。
4. 最後まで崩れが見つからなければ `return True` します。

### 例

- `[1, 2, 3]` は `True`
- `[1, 3, 2]` は `3 > 2` があるので `False`
- `[1, 1, 1]` は同じ値が続いても順序は崩れていないので `True`

### ポイント

- 全部の組み合わせを比べる必要はありません。隣同士だけを見れば十分です。
- 空リストや要素 1 個のリストは、順番が崩れる場所がないので `True` になります。

## `week10/10-1-5.py`：アナグラム判定 `is_anagram(a, b)`

### 目的

2 つの単語が、文字を並べ替えただけの関係かを判定します。

### 処理の流れ

1. `sorted(a)` で、文字列 `a` の文字をアルファベット順に並べたリストを作ります。
2. `sorted(b)` でも同じことをします。
3. 並べ替え後のリストが同じなら、使っている文字と個数が同じなのでアナグラムです。

### 例

`"tone"` と `"note"` はどちらも並べ替えると `['e', 'n', 'o', 't']` になるので `True` です。

### 注意点

- 大文字と小文字は別扱いです。`"Tone"` と `"note"` はそのままだと一致しません。
- 空白や記号も文字として扱われます。

## `week10/10-2-1.py`：乱数リストを作る `randlist(n, i, j)`

### 目的

`i` から `j` までの整数をランダムに選び、それを `n` 個並べたリストを返します。

### 処理の流れ

1. `from random import randint` で、ランダム整数を作る `randint` を読み込みます。
2. `result = []` で結果用リストを作ります。
3. `for _ in range(n):` で `n` 回繰り返します。
4. `randint(i, j)` で `i` 以上 `j` 以下の整数を 1 個作ります。
5. `append()` でリストに追加します。

### ポイント

- `randint(i, j)` は両端を含みます。つまり `randint(1, 5)` は `1, 2, 3, 4, 5` のどれかです。
- 同じ数字が何回出ても構いません。重複ありのランダムリストです。

## `week10/10-2-2.py`：重複チェック `has_duplicates(l)`

### 目的

リストに同じ値が 2 回以上出てくるかを判定します。

### 処理の流れ

1. `sorted_l = sorted(l)` で元リストを並べ替えた新しいリストを作ります。
2. 同じ値がある場合、並べ替えると必ず隣同士になります。
3. `sorted_l[i] == sorted_l[i + 1]` で隣同士を調べます。
4. 同じ値を見つけたら `True` を返します。
5. 最後まで見つからなければ `False` を返します。

### 例

`[1, 2, 3, 1]` は並べ替えると `[1, 1, 2, 3]` になり、隣に `1, 1` があるので `True` です。

### ポイント

- 元のリスト `l` は変更されません。`sorted(l)` は新しいリストを返します。
- 要素同士が比較できない混在リスト、たとえば `[1, "a"]` などでは `sorted()` がエラーになります。

## `week10/10-2-3.py`：誕生日のパラドックスのシミュレーション

### 目的

クラスに `n` 人いるとき、同じ誕生日の人が少なくとも 1 組いる確率を、ランダムシミュレーションで推定します。

### 使っている部品

- `randlist(n, 1, 365)`：`n` 人分の誕生日を 1〜365 の数字でランダムに作ります。
- `has_duplicates(...)`：同じ誕生日があるか調べます。
- `duplicate_percentage(n, trials)`：同じ実験を `trials` 回繰り返し、重複が起きた割合をパーセントで返します。

### `duplicate_percentage` の流れ

1. `count = 0` で、誕生日重複が起きた回数を数え始めます。
2. `trials` 回だけ実験します。
3. 1 回の実験では `randlist(n, 1, 365)` で `n` 人分の誕生日を作ります。
4. `has_duplicates(...)` が `True` なら `count` を 1 増やします。
5. `count / trials * 100` でパーセントに直します。

### 後半の処理

`for n in [22, 23, 40, 41]:` では、指定した人数で確率を表示します。

そのあと `for n in range(1, 70):` で人数を 1 人から順に増やし、確率が初めて 50% 以上になる人数と、90% 以上になる人数を探します。

### 注意点

- 乱数を使うので、実行するたびに結果は少し変わります。
- `trials = 100000` はかなり多いので、正確さは上がりますが実行時間も長くなります。
- うるう年は考えていません。誕生日は 365 通りとして扱っています。

## `week10/10-3-1.py`：`words.txt` をリストに読む

### 目的

単語ファイル `words.txt` を読み込み、1 行 1 単語のリスト `words` を作ります。

### 処理の流れ

1. `words = []` で空リストを作ります。
2. `open(__file__.replace("10-3-1.py", "words.txt"))` で同じフォルダの `words.txt` を開きます。
3. `for line in f:` でファイルを 1 行ずつ読みます。
4. `line.strip()` で行末の改行を取り除きます。
5. `words.append(...)` でリストに追加します。
6. 読み込み後、単語数、最初の単語、最後の単語を表示します。

### ポイント

- `strip()` をしないと、単語の末尾に `\n` が残ります。
- `with open(...) as f:` を使うと、読み終わったあとファイルが自動で閉じられます。

## `week10/10-3-2.py`：逆読みペアを探す

### 目的

`stressed` と `desserts` のように、逆から読むと別の単語になるペアを `words.txt` から探します。

### 処理の流れ

1. `words.txt` を読み込み、単語リスト `words` を作ります。
2. 各 `word` について `word[::-1]` で逆順の文字列 `revword` を作ります。
3. `if revword in words` で、その逆読み単語が辞書に存在するか確認します。
4. `word < revword` という条件で、同じペアを 2 回表示しないようにします。
5. 条件を満たすと `print(word, revword)` で表示します。

### `[::-1]` の意味

文字列スライスの書き方です。`start:stop:step` の `step` が `-1` なので、後ろから前へ 1 文字ずつ取り出します。

### 注意点

- `revword in words` はリストを先頭から探すので、単語数が多いと時間がかかります。
- 次の `10-3-3.py` では、これを二分探索で速くしています。

## `week10/10-3-3.py`：二分探索で逆読みペアを探す

### 目的

`10-3-2.py` と同じ逆読みペア探しを、二分探索 `includes(sequence, target)` を使って高速化します。

### `includes` の流れ

1. `first = 0`、`last = len(sequence) - 1` で探す範囲をリスト全体にします。
2. `while first <= last:` で、まだ探す範囲が残っている間だけ続けます。
3. `mid = (first + last) // 2` で中央の添字を求めます。
4. `elt = sequence[mid]` で中央の要素を見ます。
5. `target < elt` なら、探す値は左側にあるので `last = mid - 1` にします。
6. `target > elt` なら、探す値は右側にあるので `first = mid + 1` にします。
7. どちらでもなければ一致したので `True` を返します。
8. 見つからないまま範囲がなくなったら `False` を返します。

### 重要な前提

二分探索は、リストが昇順に並んでいる場合だけ正しく動きます。`words.txt` はソート済みの単語リストなので、この方法が使えます。

## `week10/10-4-1.py`：2-way interlocking words を探す

### 目的

1 つの単語を偶数番目の文字と奇数番目の文字に分けたとき、両方が単語になるものを探します。

### 例の考え方

`word[0::2]` は 0, 2, 4, ... 番目の文字、`word[1::2]` は 1, 3, 5, ... 番目の文字を取り出します。

もし元の単語が `abcdef` なら：

- `a = word[0::2]` は `ace`
- `b = word[1::2]` は `bdf`

この `a` と `b` がどちらも `words.txt` にあれば、interlocking word として表示します。

### 処理の流れ

1. `words.txt` を読み込みます。
2. 各 `word` について、長さが 6 未満なら飛ばします。
3. `a = word[0::2]`、`b = word[1::2]` で 2 つに分けます。
4. `len(a) >= 3 and len(b) >= 3` で、短すぎる候補を除外します。
5. `includes(words, a)` と `includes(words, b)` で両方が単語か調べます。
6. 見つけたら `print(a, b)` し、`count` を増やします。

### ポイント

- ここでも二分探索を使っているので、単語の存在確認が速いです。
- 表示しているのは元の長い単語ではなく、分解してできた 2 つの単語です。

## `week10/10-4-2.py`：3-way interlocking words を探す

### 目的

1 つの単語を 3 文字おきに分け、3 つとも単語になるものを探します。

### 切り出し方

- `a = word[0::3]`：0, 3, 6, ... 番目
- `b = word[1::3]`：1, 4, 7, ... 番目
- `c = word[2::3]`：2, 5, 8, ... 番目

### 処理の流れ

1. `words.txt` を読み込みます。
2. 長さが 9 未満の単語は飛ばします。3 つの単語を作るので、最低でも各 3 文字ほしいためです。
3. `a, b, c` に分解します。
4. 3 つとも長さ 3 以上か確認します。
5. 3 つとも `words.txt` に存在すれば表示し、数えます。

### ポイント

`10-4-1.py` の 2 分割版を、3 分割に拡張した課題です。考え方はほぼ同じで、スライスのステップが `2` から `3` になっています。

---

# Week11

Week11 は辞書、ヒストグラム、メモ化、テキスト処理の練習です。

## `week11/11-1-1.py`：文字のヒストグラム `histogram(seq)`

### 目的

文字列やリストなどの要素が、それぞれ何回出てくるかを辞書で数えます。

### 処理の流れ

1. `d = dict()` で空の辞書を作ります。
2. `for item in seq:` で、文字列なら 1 文字ずつ、リストなら 1 要素ずつ取り出します。
3. `if item not in d:` で、その要素がまだ辞書にないか調べます。
4. 初めてなら `d[item] = 0` で数える場所を作ります。
5. `d[item] = d[item] + 1` で出現回数を 1 増やします。
6. 最後に辞書 `d` を返します。

### 例

`histogram("banana")` は、おおよそ `{'b': 1, 'a': 3, 'n': 2}` のような辞書を返します。

### ポイント

- 辞書では `キー: 値` の形で情報を持ちます。
- このコードでは、キーが文字、値が出現回数です。

## `week11/11-1-2.py`：辞書をキー順に表示する `printAscending(d)`

### 目的

辞書の中身を、キーの昇順で表示します。

### 処理の流れ

1. `histogram(seq)` は `11-1-1.py` と同じです。
2. `printAscending(d)` では `sorted(d)` を使います。
3. 辞書を `sorted()` に渡すと、キーだけが昇順に並んだリストになります。
4. `for key in sorted(d):` でキーを順に取り出し、`print(key, d[key])` でキーと値を表示します。

### ポイント

- 辞書そのものは「キーから値を取り出す」ための構造です。
- 表示順を整えたいときは、`sorted(d)` のようにキーを並べ替えます。

## `week11/11-1-3.py`：ヒストグラムを棒グラフ風に表示する

### 目的

出現回数を数字ではなく `*` の本数で表示します。

### 処理の流れ

1. `histogram(seq)` で出現回数の辞書を作ります。
2. `printHistogram(d)` でキー順に取り出します。
3. `"*" * d[key]` で、出現回数ぶんの `*` を作ります。
4. `print(key, "*" * d[key])` で、文字と棒を表示します。

### 例

辞書が `{"a": 3, "b": 1}` なら、表示は次のようなイメージです。

```text
a ***
b *
```

### ポイント

Python では文字列に整数を掛けると、その文字列を繰り返した新しい文字列になります。

## `week11/11-1-4.py`：`dict.get()` を使った短いヒストグラム

### 目的

`11-1-1.py` の `if item not in d:` を、`get()` を使って短く書きます。

### 処理の流れ

`d[item] = d.get(item, 0) + 1` が中心です。

- `d.get(item, 0)` は、`item` が辞書にあればその値を返します。
- `item` がなければ、代わりに `0` を返します。
- そこに `+ 1` するので、初登場なら `1`、2 回目以降なら前の回数 + 1 になります。

### ポイント

この書き方は、辞書で数を数えるときによく使います。`if` を書かずに「なければ 0」と扱えるのが便利です。

## `week11/11-2-1.py`：英語から日本語読みへの辞書 `e2j`

### 目的

英語の数字単語を、日本語の読みへ対応させる辞書を作ります。

### 中身

```python
e2j = {
    "one": "ichi",
    "two": "ni",
    "three": "san",
    "four": "yon",
    "five": "go",
}
```

左側がキー、右側が値です。たとえば `e2j["three"]` は `"san"` です。

### ポイント

- 辞書は「検索したいもの」をキーにします。
- この場合、英語を入力して日本語読みを取り出したいので、英語がキーです。

## `week11/11-2-2.py`：1 対 1 の辞書を逆向きにする `reverseMap(d)`

### 目的

`{"one": "ichi"}` のような辞書を、`{"ichi": "one"}` のように逆向きにします。

### 処理の流れ

1. `result = dict()` で空の辞書を作ります。
2. `for key in d:` で元の辞書のキーを取り出します。
3. `d[key]` で元の値を取り出します。
4. `result[d[key]] = key` で、元の値を新しいキー、元のキーを新しい値にします。
5. 最後に `result` を返します。

### 注意点

この書き方が安全に使えるのは、元の値が重複していない場合です。もし `{"a": 1, "b": 1}` を逆にすると、キー `1` が重複して片方が上書きされます。

## `week11/11-2-3.py`：多対 1 の辞書を逆向きにする

### 目的

同じ値を持つキーが複数ある辞書を、情報を失わずに逆向きにします。

### なぜリストが必要か

ヒストグラムでは、たとえば複数の文字が同じ回数出ることがあります。

```python
{"a": 3, "b": 1, "c": 3}
```

これを逆向きにすると、`3` に対応する文字は `"a"` と `"c"` の 2 つです。そこで値をリストにして、`{3: ["a", "c"], 1: ["b"]}` のようにします。

### `reverseMap(d)` の流れ

1. `result = dict()` で空辞書を作ります。
2. 各 `key` について `value = d[key]` を取り出します。
3. `if value not in result:` で、その回数のリストがまだないか調べます。
4. なければ `result[value] = []` で空リストを作ります。
5. `result[value].append(key)` で、その回数に対応するキーを追加します。

### ポイント

- `result[value]` の値はリストです。
- 同じ回数の文字を全部保存できるので、上書きによる情報消失が起きません。

## `week11/11-3-1.py`：フィボナッチ数のメモ化

### 目的

普通の再帰でフィボナッチ数を求める場合と、メモ化を使う場合の速度差を比べます。

### `fib_without_memo(n)` の流れ

1. `if n < 2:` なら `1` を返します。
2. それ以外なら `fib_without_memo(n - 1) + fib_without_memo(n - 2)` を返します。

この関数は同じ値を何度も再計算します。たとえば `fib(32)` を求める途中で `fib(30)`、`fib(29)` などが何度も呼ばれます。

### `memo` の役割

`memo = dict()` は、一度計算した答えを保存する辞書です。

キーは `n`、値は `fib(n)` の結果です。たとえば `memo[10] = 89` のように保存します。

### `fib(n)` の流れ

1. `if n in memo:` で、すでに答えを計算済みか調べます。
2. 計算済みなら `return memo[n]` で即返します。
3. 未計算なら、普通に再帰で計算します。
4. 計算結果を `memo[n] = result` で保存します。
5. `return result` で答えを返します。

### 後半の測定

- `time.time()` で現在時刻を秒で取得します。
- 計算前後の差を取ることで、処理時間を測ります。
- `memo.clear()` は、測定前にメモを空にして条件を整えるためです。
- `slow_time / fast_time` で、メモ化版が何倍速いかを出しています。

### 注意点

- このコードのフィボナッチは `fib(0) = 1`、`fib(1) = 1` です。数学でよく使う `fib(0) = 0` とは少し違います。
- `fast_time` が非常に小さい場合、速度倍率はかなり大きく表示されます。

## `week11/11-4-1.py`：文字または空白か調べる `isLetter(c)`

### 目的

文字 `c` がアルファベットなどの文字、または空白なら `True`、記号なら `False` を返します。

### 処理

```python
return c.isalpha() or c.isspace()
```

- `c.isalpha()` は、`c` が文字なら `True` です。
- `c.isspace()` は、空白、改行、タブなどなら `True` です。
- `or` なので、どちらか片方でも `True` なら全体が `True` です。

### なぜ空白を残すのか

後で文章を単語に分けるとき、空白が消えてしまうと単語同士がくっつきます。たとえば `"hello, world"` から空白まで消すと `"helloworld"` になってしまいます。

## `week11/11-4-2.py`：句読点を取り除く `depunctuate(word)`

### 目的

文章からコンマ、ピリオド、引用符などの記号を取り除きます。ただし単語の区切りに必要な空白は残します。

### `depunctuate` の流れ

```python
return "".join(filter(isLetter, word))
```

1. `filter(isLetter, word)` で、`word` の各文字について `isLetter` が `True` のものだけを残します。
2. `join(...)` で、残った文字を 1 つの文字列に戻します。
3. `""` は区切り文字なしで結合するという意味です。

### ファイル処理

`text2.txt` を 1 行ずつ読み、各行に `depunctuate(line)` をかけて表示します。

`print(..., end="")` としているのは、元の行に含まれる改行をそのまま使い、`print` が余分な改行を足さないようにするためです。

### 注意点

- 数字は `isalpha()` でも `isspace()` でもないので削除されます。
- `isalpha()` は英字だけでなく、日本語などの文字にも `True` を返すことがあります。

## `week11/11-4-3.py`：ファイル内の単語頻度を数える

### 目的

`text2.txt` の文章を読み、単語ごとの出現回数を辞書で数えます。

### 処理の流れ

1. `h = dict()` でヒストグラム用の辞書を作ります。
2. `text2.txt` を 1 行ずつ読みます。
3. `line = depunctuate(line)` で記号を削除します。
4. `for word in line.split():` で、空白区切りで単語に分けます。
5. `word = word.lower()` で小文字に統一します。
6. `h[word] = h.get(word, 0) + 1` で単語の回数を増やします。
7. `printAscending(h)` で単語をアルファベット順に表示します。
8. 最後に `"do"` と `"know"` の回数だけ個別に表示します。

### なぜ `lower()` が必要か

`"Do"` と `"do"` を別の単語として数えないためです。小文字にそろえることで、同じ単語として扱えます。

### ポイント

これは Week11 の辞書練習の総合版です。記号除去、単語分割、小文字化、ヒストグラム作成、ソート表示を組み合わせています。

## `week11/11-5.py`：行数・単語数・文字数を数える

### 目的

`text2.txt` の行数、単語数、文字数を数えて表示します。

### 処理の流れ

1. `lines = 0`、`words = 0`、`characters = 0` でカウンタを用意します。
2. `text2.txt` を 1 行ずつ読みます。
3. 1 行読むたびに `lines = lines + 1` で行数を増やします。
4. `len(line.split())` で、その行の単語数を数え、`words` に足します。
5. `len(line)` で、その行の文字数を数え、`characters` に足します。
6. 最後に 3 つの結果を表示します。

### 注意点

- `len(line)` は改行文字 `\n` も 1 文字として数えます。
- `line.split()` は連続する空白をまとめて区切りとして扱います。
- このコードでは句読点を消していないので、`"word,"` のようなものも 1 単語として数えます。

---

# Week12

Week12 はテキストファイルを読み、ランダム選択やマルコフ連鎖で文章を生成する課題です。

## `week12/ip1w12p01.py`：本文からランダムに 20 語を表示する

### 目的

`text3.txt` に含まれるすべての単語をリストにし、その中からランダムに 20 語を選んで表示します。

### `fileWords(fileName)` の流れ

1. `allWords = []` で、全単語を入れる空リストを作ります。
2. `with open(fileName, encoding="utf-8") as file:` で UTF-8 としてファイルを開きます。
3. `for line in file:` で 1 行ずつ読みます。
4. `words = line.split()` で、その行を空白区切りの単語リストにします。
5. `allWords.extend(words)` で、その行の単語をまとめて追加します。
6. 最後に `allWords` を返します。

### `append` ではなく `extend` を使う理由

`words` は「その行の単語リスト」です。

- `append(words)` にすると、リストの中にリストが入ります。
- `extend(words)` にすると、単語を 1 個ずつ `allWords` に追加できます。

### 後半の処理

```python
words = fileWords(__file__.replace("ip1w12p01.py", "text3.txt"))
```

同じフォルダにある `text3.txt` を読み、全単語リストを作ります。

```python
for _ in range(20):
    word = random.choice(words)
    print(word)
```

この部分で、単語リストから 1 語をランダムに選ぶ処理を 20 回繰り返します。

### 注意点

- `random.choice(words)` は同じ単語を再び選ぶ可能性があります。
- `split()` だけなので、ピリオドやコンマは単語にくっついたままです。
- このファイルには `if __name__ == "__main__":` がないので、import しただけでも実行されます。

## `week12/ip1w12p02.py`：マルコフモデルで文章を作る

### 目的

`text3.txt` の単語の並び方をもとに、「この 2 語の次にはどんな単語が来やすいか」という辞書を作り、ランダムに文を生成します。

### `prefixlen = 2`

`prefixlen` は、次の単語を選ぶときに何語分の前文脈を見るかを表します。

このコードでは `2` なので、2 語の組をキーにします。たとえば本文に `I am happy today` があれば：

- prefix：`("I", "am")`
- suffix：`"happy"`

という対応を記録します。

### `fileWords(fileName)`

`ip1w12p01.py` と同じ考え方です。本文を読み、単語が順番に並んだ 1 つのリストを返します。マルコフモデルでは単語の順番が重要なので、読み込んだ順序を保っています。

### `buildMarkov(words, prefixlen)` の流れ

1. `markov = {}` で空の辞書を作ります。
2. `for i in range(len(words) - prefixlen):` で、prefix と suffix を作れる位置だけを順に見ます。
3. `prefix = tuple(words[i:i + prefixlen])` で、連続する `prefixlen` 語をタプルにします。
4. `suffix = words[i + prefixlen]` で、その直後の 1 語を取り出します。
5. `markov[prefix] = markov.get(prefix, []) + [suffix]` で、prefix に続いた suffix を候補リストに追加します。
6. 最後に `markov` を返します。

### なぜ prefix はタプルなのか

辞書のキーには、変更可能なリストを使えません。`words[i:i + prefixlen]` はリストなので、そのままではキーにできません。

そこで `tuple(...)` で変更不能なタプルに変換しています。タプルなら辞書キーとして使えます。

### suffix 候補がリストである理由

同じ prefix が本文中に何回も出ることがあります。そのたびに直後の単語をリストへ追加します。

もし同じ suffix が何度も追加されれば、`random.choice` で選ばれる確率も高くなります。つまり、本文でよくある続き方ほど選ばれやすくなります。

### `startsWithCapital(prefix)`

文の始まりらしい prefix を探すための関数です。

1. `first = prefix[0]` で prefix の最初の単語を取ります。
2. `len(first) > 0` で空文字でないことを確認します。
3. `first[0].isupper()` で最初の文字が大文字か確認します。

大文字で始まる単語は文頭である可能性が高いため、生成文の開始地点として使っています。

### `randomCapitalPrefix(markov)`

1. `list(markov.keys())` で、prefix の一覧を作ります。
2. `random.choice(...)` でランダムに 1 つ選びます。
3. `startsWithCapital(prefix)` が `True` なら、その prefix を返します。
4. 大文字で始まらなければ、もう一度選び直します。

### 注意点

もし大文字で始まる prefix が 1 つもなければ、この `while True` は終わりません。ただし普通の英文テキストなら文頭の大文字があるので問題になりにくいです。

### `makeSentence(markov)` の流れ

1. `prefix = randomCapitalPrefix(markov)` で、文頭らしい 2 語を選びます。
2. `result = list(prefix)` で、生成結果の単語リストを作ります。
3. `result[-1].endswith(".")` なら、選んだ prefix の時点で文が終わっているので、そのまま返します。
4. `for _ in range(200):` で最大 200 語まで続けます。
5. `suffixes = markov.get(prefix)` で、今の prefix に続けられる単語候補を取ります。
6. 候補がなければ `break` し、外側の `while True` で最初からやり直します。
7. `suffix = random.choice(suffixes)` で候補から 1 語選びます。
8. `result.append(suffix)` で文に追加します。
9. `suffix.endswith(".")` なら、ピリオドで文が終わったので `" ".join(result)` で文字列にして返します。
10. 終わっていなければ `prefix = prefix[1:] + (suffix,)` で prefix を 1 語分ずらします。

### prefix の更新

たとえば現在の prefix が `("I", "am")` で、選んだ suffix が `"happy"` なら、次の prefix は `("am", "happy")` になります。

`prefix[1:]` は先頭を落としたタプルです。そこに `(suffix,)` という 1 要素タプルを足して、新しい 2 語 prefix を作っています。

### 文章生成の特徴

- 元の本文に実際に存在した「2 語の後に来た単語」だけを使うので、完全なデタラメよりは自然になります。
- ただし全体の意味を理解しているわけではありません。
- ピリオドで終わる単語が出るまで続けます。
- `?` や `!` では終了しません。このコードの終了条件は `.` だけです。
- 最大 200 語で一度あきらめ、最初の prefix 選びからやり直します。

### 最後の 3 行

```python
words = fileWords(__file__.replace("ip1w12p02.py", "text3.txt"))
markov = buildMarkov(words, prefixlen)
print(makeSentence(markov))
```

1. `text3.txt` を単語リストとして読みます。
2. 単語リストからマルコフ辞書を作ります。
3. その辞書を使って 1 文生成し、表示します。

### `text4.txt` について

`week12` フォルダには `text4.txt` もありますが、現在の 2 つの Python ファイルはどちらも `text3.txt` を読んでいます。`text4.txt` はこのコードでは使われていません。

---

# まとめ

- Week10 は、リスト処理、スライス、重複判定、二分探索、ランダムシミュレーションが中心です。
- Week11 は、辞書を使ったカウント、辞書の逆引き、メモ化、テキスト処理が中心です。
- Week12 は、ファイルから単語列を作り、ランダム選択やマルコフモデルでテキストを生成します。

特に大事な考え方は、Week10 の「リストを順番に処理する」、Week11 の「辞書で対応や回数を管理する」、Week12 の「単語の並びをデータとして利用する」の 3 つです。
